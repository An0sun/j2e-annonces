# ===== Production Profile =====
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

logging:
  level:
    com.masterannonce: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
